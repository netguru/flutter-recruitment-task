name: filmguru
description: Netguru Flutter recrutation app
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_bloc: ^7.3.0
  bloc: ^7.2.1
  equatable: ^2.0.3
  json_annotation: ^4.1.0
  cupertino_icons: ^1.0.3
  dio: ^4.0.0
  freezed_annotation: ^0.14.3
  dartz: ^0.10.0
  rxdart: ^0.27.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^5.0.2
  build_runner: ^2.1.4
  build_test: ^2.1.4
  mockito: ^5.0.16
  bloc_test: ^8.2.0
  freezed: ^0.14.5

flutter:
  uses-material-design: true

  assets:
    - assets/mock/movies_response.json

flutter_intl:
  enabled: true
